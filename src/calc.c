#include <stdio.h>
#include <math.h>
#include "calc.h"

float arrel(double a)
{
    return sqrt(a);
}
float resta(int a, int b)
{
    return a - b;
}
float divisio(double a, double b)
{
    return a / b;
}